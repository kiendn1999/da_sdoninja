import 'package:flutter/material.dart';

class PartnerMessageScreen extends StatelessWidget {
  const PartnerMessageScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Partner message"),);
  }
}